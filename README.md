### This repository fine-tunes the OpenAI ada model on custom dataset and compare its results with scikit-learn models.

#### The accuracy of Ada is same as that of sklearn models (with default hyperparameters)

